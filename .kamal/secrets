# Committed file — reference ENV / files only, never raw credentials.
# Export before deploying:
#   export KAMAL_REGISTRY_PASSWORD=ghp_...        # GitHub PAT with write:packages
#   export POSTGRES_PASSWORD_KYUFY=...            # this app's database password
#   export KYUFY_DEPLOY_HOST=...                  # the Hetzner IP (see docs/LOCAL.md)
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
RAILS_MASTER_KEY=$(cat config/master.key)
POSTGRES_PASSWORD=$POSTGRES_PASSWORD_KYUFY
